context:
  version: "100.0.0"

package:
  name: robotiq_description
  version: ${{ version }}

source:
  git: https://github.com/JafarAbdi/ros2_robotiq_gripper.git
  tag: ${{ version }}
  patches:
    - delete_ament.patch

build:
  number: 1
  script: |
    cmake ${CMAKE_ARGS} -GNinja -DBUILD_TESTS=OFF \
          -DCMAKE_INSTALL_PREFIX=$PREFIX \
          $SRC_DIR/robotiq_description
    ninja install

requirements:
  build:
    - ${{ compiler('cxx') }}
    - cmake
    - ninja
